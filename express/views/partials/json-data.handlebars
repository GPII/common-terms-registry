fluid.defaults("ctr.components.data", {
    gradeNames: ["fluid.modelComponent", "autoInit"],
    model: {
        user: {{#if user }}{ name: "{{user.name}}"}{{else}}undefined{{/if}},
        record: {{#if record }}{ uniqueId: "{{record.uniqueId}}"}{{else}}undefined{{/if}}
    }
});
var data = ctr.components.data();