{{#if user }}
    fluid.registerNamespace("ctr.data.user");
    ctr.data.user = {
        name: "{{user.name}}"
    };
{{/if}}

{{#if record }}
    fluid.registerNamespace("ctr.data.record");
    ctr.data.record = {
        uniqueId: "{{record.uniqueId}}"
    };
{{/if}}


