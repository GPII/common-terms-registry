fluid.defaults("ctr.components.data", {
    gradeNames: ["fluid.modelRelayComponent", "autoInit"],
    model: {
        {{#if path}}path: "{{path}}",{{/if}}
        {{#if user}}{{>json-data-user}},{{/if}}
        {{#if query}}{{>json-data-query}},{{/if}}
        {{#if cookies}}cookies: {{{cookies}}},{{/if}}
        {{>json-data-record}}
    }
});
var data = ctr.components.data();

