record: { {{~#if record.aliasOf}}aliasOf: "{{record.aliasOf}}",{{/if}}
{{~#if record.applicationUniqueFlag}}applicationUniqueFlag: "{{record.applicationUniqueFlag}}",{{/if}}
{{~#if record.definition}}definition: "{{record.definition}}",{{/if}}
{{~#if record.namespace}}namespace: "{{record.namespace}}",{{/if}}
{{~#if record.notes}}notes: "{{record.notes}}",{{/if}}
{{~#if record.permanency}}permanency: "{{record.permanency}}",{{/if}}
{{~#if record.status}}status: "{{record.status}}",{{/if}}
{{~#if record.defaultValue}}defaultValue: "{{record.defaultValue}}",{{/if}}
{{~#if record.termLabel}}termLabel: "{{record.termLabel}}",{{/if}}
{{~#if record.translationOf}}translationOf: "{{record.translationOf}}",{{/if}}
{{~#if record.type}}type: "{{record.type}}",{{/if}}
{{~#if record.updated}}updated: "{{record.updated}}",{{/if}}
{{~#if record.uses}}uses: [{{#each record.uses}}{{#if @index}}, {{/if}}"{{.}}"{{/each}}]{{/if}}
{{~#if record.valueSpace}}valueSpace: "{{record.valueSpace}}",{{/if}}
uniqueId: "{{~record.uniqueId}}"
}
