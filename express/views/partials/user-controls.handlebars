<div id="profile">
{{#if user}}
    <a href="#" data-dropdown="user-dropdown" class="button expand dropdown secondary small transparent" tabindex="0">
        <span class="ptd-icon-user"></span>
        {{#if user}}
            {{#if user.displayName}}{{user.displayName}}{{else}}{{user.name}}{{/if}}
        {{else}}
            Not Logged In
        {{/if}}
    </a>
    <br/>
    <ul id="user-dropdown" data-dropdown-content class="f-dropdown user-menu">
        <li class="user-menu-logout" {{#unless user}}style="display:none"{{/unless}} tabindex="-1">Log Out</li>
        <li class="user-menu-profile" {{#unless user}}style="display:none"{{/unless}} tabindex="-1"><a href="/profile">Profile</a></li>
    </ul>
{{else}}
    <div class="ptd-not-logged-in-instructions">
        <p>You must log in to contribute to the Preference Terms Dictionary.</p>
        <p><a href="/login{{#if path}}?source={{path}}{{/if}}" tabindex="0">Log in</a> or <a href="/signup{{#if path}}?source={{path}}{{/if}}" tabindex="0">Create an account</a>.</p>
    </div>
{{/if}}
</div>